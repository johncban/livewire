<% if @post.user %>
  <div id="post-<%= @post.id %>" class="post">
    <div class="row">
      <div class="col-lg-6 col-lg-offset-3 well">
        <span class="text"><%= @post.post_content_html %></span>
      </div>
    </div>
    <% if @post.user_id == current_user.id %>
     <%= link_to 'Destroy', user_post_path(@post), method: :delete, data: { confirm: 'Are you sure?' } %>
    <% end %>
    <div>
      <div>
        
        <hr>
        <div class="comments">
          <%= render partial: "comments/template", locals: {commentable: @post, new_comment: @new_comment} %>
        </div>
      </div>
    </div>
  </div>
<% end %>
